Remove logo image and replace with this backgrouns image.
Apply these changes inside the existing hero/landing section component only.
Modify the existing homepage hero section only.

Keep the current layout, text content, navbar, colors, and CTA button unchanged.

Add a full-width background image public/taj-mahal.png (Taj Mahal image) to the hero section using CSS.

Apply a soft cream/white gradient overlay on top of the background image to maintain readability.

Hero Section Layout:

Left side: keep the existing heading, description, and “Explore Collection” button.

Right side: add the existing owner portrait image aligned to the right.

Ensure the portrait image appears above the background (higher z-index).

Styling details:

Background image slightly blurred or desaturated.

Portrait image should be sharp, with subtle shadow and soft edges.

Use position: relative and ::before overlay OR linear-gradient over background-image.

Maintain premium, minimal, luxury boutique aesthetics.